/** The key legend, shared verbatim by the live (`main.ts`) and rehearsal (`rehearsal-main.ts`) entrypoints so the two screens read identically. */
export const LEGEND = 'ASSAY  [1] discover  [2] pay  [3] serve  [4] challenge  [q] quit';

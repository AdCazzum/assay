/**
 * @assay/demo — the keyboard-driven demo runner (issue #86).
 *
 * Composes the real adapters, `@assay/core`'s loop, the rug-score capability
 * and `@assay/dashboard`'s renderer into one screen you step through on
 * keypress. See `docs/demo-run-sheet.md` for the running order this implements,
 * which is measured rather than invented.
 *
 * Implementation lands with #86.
 */

export const APP_ID = '@assay/demo';

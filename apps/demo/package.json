{
  "name": "@assay/demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "description": "A real Claude agent driving the Assay MCP server, its reasoning and the live loop shown side by side",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@assay/cap-rugscore": "workspace:*",
    "@assay/core": "workspace:*",
    "@assay/dashboard": "workspace:*",
    "@assay/graph": "workspace:*",
    "@assay/payments": "workspace:*",
    "@assay/registry": "workspace:*"
  }
}

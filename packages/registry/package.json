{
  "name": "@assay/registry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "description": "ENS adapter (Sepolia): capability manifest + portable reputation text records",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@assay/core": "workspace:*",
    "@assay/payments": "workspace:*",
    "ethers": "^6.17.0"
  }
}

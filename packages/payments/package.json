{
  "name": "@assay/payments",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "description": "Hedera adapter (testnet): pay, bond, slash, confirm",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@assay/core": "workspace:*",
    "@hashgraph/sdk": "^2.81.0"
  }
}
